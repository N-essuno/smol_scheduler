package org.smolang.greenhouse.scheduler;

/**
 * Enum for the different types of models that can be used in the scheduler. As of now, it is only
 * used to differentiate the prefixes to use.
 */
public enum ModelTypeEnum {
  ASSET_MODEL,
  SMOL_MODEL
}
